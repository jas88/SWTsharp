---
phase: 01-test-infrastructure-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/SWTSharp.Tests/Infrastructure/PlatformFactAttributes.cs
  - tests/SWTSharp.Tests/SWTSharp.Tests.csproj
  - .github/workflows/ci.yml
autonomous: true

must_haves:
  truths:
    - "Platform-specific tests only run on their target platform"
    - "[WindowsFact] tests are not discovered on macOS or Linux"
    - "[MacOSFact] tests are not discovered on Windows or Linux"
    - "[LinuxFact] tests are not discovered on Windows or macOS"
    - "CI runs dotnet test (not dotnet run) with proper test discovery"
  artifacts:
    - path: "tests/SWTSharp.Tests/Infrastructure/PlatformFactAttributes.cs"
      provides: "Platform-specific test attributes with discoverers"
      exports: ["WindowsFactAttribute", "MacOSFactAttribute", "LinuxFactAttribute"]
      contains: "IXunitTestCaseDiscoverer"
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow using dotnet test"
      contains: "dotnet test"
  key_links:
    - from: "tests/SWTSharp.Tests/Infrastructure/PlatformFactAttributes.cs"
      to: "xunit.extensibility.execution"
      via: "IXunitTestCaseDiscoverer implementation"
      pattern: "XunitTestCaseDiscoverer"
    - from: ".github/workflows/ci.yml"
      to: "tests/SWTSharp.Tests"
      via: "dotnet test command"
      pattern: "dotnet test.*SWTSharp.Tests"
---

<objective>
Add platform-specific test attributes and fix CI to use proper dotnet test discovery.

Purpose: The current CI runs `dotnet run` which bypasses VSTest discovery and the custom adapter. Platform tests can crash when run on wrong platform. Tests need [WindowsFact], [MacOSFact], [LinuxFact] attributes that filter at discovery time.

Output: Platform-specific attributes that return empty IEnumerable on wrong platform (tests not discovered, not skipped), and CI workflow using `dotnet test`.
</objective>

<execution_context>
@/Users/jas88/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jas88/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-test-infrastructure-foundation/01-RESEARCH.md

Existing infrastructure:
@tests/SWTSharp.Tests/Infrastructure/TestBase.cs
@tests/SWTSharp.Tests/SWTSharp.Tests.csproj
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create platform-specific test attributes</name>
  <files>tests/SWTSharp.Tests/Infrastructure/PlatformFactAttributes.cs</files>
  <action>
Create a new file with platform-specific fact attributes using xUnit's IXunitTestCaseDiscoverer pattern.

Key requirements from RESEARCH.md Pitfall 5:
- Return Enumerable.Empty&lt;IXunitTestCase&gt;() when NOT on target platform (NOT a skipped test case)
- Return actual XunitTestCase when ON target platform

Create three attributes and their discoverers:
1. [WindowsFact] + WindowsFactDiscoverer
2. [MacOSFact] + MacOSFactDiscoverer
3. [LinuxFact] + LinuxFactDiscoverer

Each attribute should:
- Inherit from FactAttribute
- Have [XunitTestCaseDiscoverer] attribute pointing to its discoverer

Each discoverer should:
- Implement IXunitTestCaseDiscoverer
- Take IMessageSink in constructor (diagnosticMessageSink)
- Check RuntimeInformation.IsOSPlatform(OSPlatform.X)
- If correct platform: return new[] { new XunitTestCase(...) }
- If wrong platform: return Enumerable.Empty&lt;IXunitTestCase&gt;()

Follow the exact pattern from RESEARCH.md "Platform-Specific Test Attribute" code example (lines 626-661).

CRITICAL: Use Enumerable.Empty, not a test case with Skip reason. Skipped tests still appear in results and can cause confusion.
  </action>
  <verify>
Build: `dotnet build tests/SWTSharp.Tests/SWTSharp.Tests.csproj`
Check that PlatformFactAttributes.cs compiles without errors.
  </verify>
  <done>
WindowsFactAttribute, MacOSFactAttribute, and LinuxFactAttribute exist with corresponding discoverers that return empty enumerable on wrong platform.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CI workflow to use dotnet test</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Update the CI workflow to use `dotnet test` instead of `dotnet run` for test execution.

Changes needed for all three platform jobs (test-windows, test-macos, test-linux):

1. Replace the test run step:
   FROM: `dotnet run --project tests/SWTSharp.Tests/SWTSharp.Tests.csproj --configuration ${{ env.CONFIGURATION }}`
   TO: `dotnet test tests/SWTSharp.Tests/SWTSharp.Tests.csproj --configuration ${{ env.CONFIGURATION }} --no-build --logger "trx;LogFileName=test-results.trx"`

2. For macOS specifically, keep the timeout-minutes: 5

3. Update macos runner from macos-latest to macos-15 per CONTEXT.md decision: "Runners: windows-latest, macos-15 (Sequoia), ubuntu-latest"

4. Add job timeout: 15 minutes per CONTEXT.md: "Job timeout: 15 minutes"
   Add `timeout-minutes: 15` at job level for all three jobs

5. Change trigger to only run on PRs + main branch per CONTEXT.md:
   FROM: push branches [main, develop]
   TO: push branches [main]
   (remove develop from push trigger, keep in pull_request)

6. Add NuGet cache step per CONTEXT.md: "Cache NuGet packages for faster builds"
   Add actions/cache@v4 for ~/.nuget/packages with cache key based on packages.lock.json

Keep existing steps for:
- Codecov upload (directory, flags, name)
- Test results artifact upload
- Linux-specific Xvfb setup
- Linux-specific CodeQL analysis
  </action>
  <verify>
Validate YAML syntax: Check that the workflow file is valid YAML.
Verify all three jobs have dotnet test command and 15-minute timeout.
  </verify>
  <done>
CI workflow uses dotnet test for all platforms, has 15-minute job timeouts, uses macos-15 runner, and caches NuGet packages.
  </done>
</task>

</tasks>

<verification>
After completing both tasks:

1. Build test project:
   ```bash
   dotnet build tests/SWTSharp.Tests/SWTSharp.Tests.csproj
   ```

2. Run tests locally to verify attributes work:
   ```bash
   dotnet test tests/SWTSharp.Tests/SWTSharp.Tests.csproj --list-tests
   ```
   Platform-specific tests should only appear when on correct platform.

3. Validate CI workflow YAML syntax:
   ```bash
   # Check yaml is valid
   python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"
   ```
</verification>

<success_criteria>
- PlatformFactAttributes.cs exists with WindowsFact, MacOSFact, LinuxFact
- Each attribute has corresponding discoverer returning empty on wrong platform
- CI workflow uses `dotnet test` (not `dotnet run`)
- CI workflow has 15-minute job timeouts
- CI workflow uses macos-15 runner
- CI workflow has NuGet cache
- Build succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure-foundation/01-02-SUMMARY.md`
</output>
