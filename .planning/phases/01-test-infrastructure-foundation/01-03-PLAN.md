---
phase: 01-test-infrastructure-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - tests/SWTSharp.Tests/Infrastructure/EventSyncHelpers.cs
  - tests/SWTSharp.Tests/Infrastructure/GUITestBase.cs
  - tests/SWTSharp.Tests/SWTSharp.Tests.csproj
  - .github/workflows/ci.yml
autonomous: true

must_haves:
  truths:
    - "Event-based synchronization uses TaskCompletionSource with RunContinuationsAsynchronously"
    - "No polling loops (Thread.Sleep in while loop) exist in test helpers"
    - "Coverage is collected on all three platforms"
    - "GUITestBase manages Display creation/disposal for GUI tests"
  artifacts:
    - path: "tests/SWTSharp.Tests/Infrastructure/EventSyncHelpers.cs"
      provides: "TaskCompletionSource-based event synchronization"
      contains: "TaskCreationOptions.RunContinuationsAsynchronously"
      exports: ["WaitForEvent", "WaitForCondition"]
    - path: "tests/SWTSharp.Tests/Infrastructure/GUITestBase.cs"
      provides: "Base class for GUI tests with Display management"
      contains: "Display"
    - path: ".github/workflows/ci.yml"
      provides: "Coverage collection configuration"
      contains: "CollectCoverage=true"
  key_links:
    - from: "tests/SWTSharp.Tests/Infrastructure/EventSyncHelpers.cs"
      to: "System.Threading.Tasks.TaskCompletionSource"
      via: "TCS with RunContinuationsAsynchronously flag"
      pattern: "TaskCompletionSource.*RunContinuationsAsynchronously"
    - from: ".github/workflows/ci.yml"
      to: "coverlet.collector"
      via: "MSBuild coverage parameters"
      pattern: "/p:CollectCoverage=true"
---

<objective>
Add event-based synchronization helpers and configure coverage collection across all platforms.

Purpose: Current TestHelpers.WaitFor uses polling (Thread.Sleep in while loop) which is flaky and wastes CPU. TaskCompletionSource-based synchronization is the correct pattern. Coverage collection is configured but not actively collecting due to custom test runner usage.

Output: EventSyncHelpers with TaskCompletionSource patterns, GUITestBase for GUI test management, and working Coverlet coverage configuration.
</objective>

<execution_context>
@/Users/jas88/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jas88/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-test-infrastructure-foundation/01-RESEARCH.md

Existing infrastructure:
@tests/SWTSharp.Tests/Infrastructure/TestHelpers.cs
@tests/SWTSharp.Tests/Infrastructure/TestBase.cs
@tests/SWTSharp.Tests/SWTSharp.Tests.csproj
@.github/workflows/ci.yml

Prior plan outputs:
@.planning/phases/01-test-infrastructure-foundation/01-01-SUMMARY.md
@.planning/phases/01-test-infrastructure-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EventSyncHelpers with TaskCompletionSource pattern</name>
  <files>tests/SWTSharp.Tests/Infrastructure/EventSyncHelpers.cs</files>
  <action>
Create a new EventSyncHelpers.cs file with TaskCompletionSource-based synchronization helpers.

CRITICAL requirement from RESEARCH.md Pitfall 2:
- ALWAYS use TaskCreationOptions.RunContinuationsAsynchronously
- Without this flag, deadlocks occur when continuations try to acquire locks held by SetResult caller

Methods to create:

1. WaitForEvent&lt;T&gt;(Action&lt;Action&lt;T&gt;&gt; subscribe, Action&lt;Action&lt;T&gt;&gt; unsubscribe, TimeSpan? timeout = null)
   - Default timeout: 5 seconds (per CONTEXT.md)
   - Subscribe to event, signal TCS on event fire, unsubscribe
   - Dispose timer on completion or timeout
   - Return Task&lt;T&gt; that completes when event fires or times out with TimeoutException

2. WaitForCondition(Func&lt;bool&gt; condition, TimeSpan? timeout = null)
   - Async-friendly version of current WaitFor that doesn't poll
   - Use Timer with periodic callback checking condition
   - Complete TCS when condition becomes true
   - Throw TimeoutException on timeout

3. WaitForEventCount&lt;T&gt;(Action&lt;Action&lt;T&gt;&gt; subscribe, Action&lt;Action&lt;T&gt;&gt; unsubscribe, int count, TimeSpan? timeout = null)
   - Wait for event to fire N times
   - Return List&lt;T&gt; of all captured event args

Follow the exact pattern from RESEARCH.md "Event-Based Synchronization Helper" code example (lines 579-617).

Add XML documentation for each method explaining the timeout behavior and exception types.
  </action>
  <verify>
Build: `dotnet build tests/SWTSharp.Tests/SWTSharp.Tests.csproj`
Verify EventSyncHelpers.cs compiles and contains RunContinuationsAsynchronously.
  </verify>
  <done>
EventSyncHelpers.cs exists with WaitForEvent, WaitForCondition, WaitForEventCount methods using TaskCompletionSource with RunContinuationsAsynchronously flag.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GUITestBase for GUI test management</name>
  <files>tests/SWTSharp.Tests/Infrastructure/GUITestBase.cs</files>
  <action>
Create GUITestBase class that manages Display creation/disposal for GUI tests per CONTEXT.md decisions.

Requirements:
1. Inherit from IDisposable, IAsyncLifetime (for xUnit async setup/teardown)
2. Manage Display lifecycle:
   - Default: Use shared Display for speed (DisplayFixture pattern)
   - Opt-in: Fresh Display for isolation (constructor parameter)
3. Strict disposal checking per CONTEXT.md:
   - Tests MUST fail if widgets not disposed at end of test
   - Add Dispose check that throws if any Shell exists with non-disposed children
4. Event queue checking per CONTEXT.md:
   - Tests fail if event queue left dirty (pending events)
   - Call Display.ReadAndDispatch() in loop, fail if events still pending
5. Failure diagnostics per CONTEXT.md:
   - On failure, attempt to capture: screenshot + widget tree
   - Store in TestResults folder for artifact upload

Key methods:
- InitializeAsync(): Create/acquire Display
- DisposeAsync(): Verify disposal, capture diagnostics on failure
- CreateShell(): Create Shell with auto-tracking
- RunOnUIThread(Action): Execute on UI thread via Display.SyncExec
- RunOnUIThread&lt;T&gt;(Func&lt;T&gt;): Execute and return result

GUI tests should inherit from GUITestBase instead of TestBase.

Add [Collection("GUI Tests")] with DisableParallelization = true per RESEARCH.md Pattern 5.
  </action>
  <verify>
Build: `dotnet build tests/SWTSharp.Tests/SWTSharp.Tests.csproj`
Verify GUITestBase.cs compiles and contains disposal checking logic.
  </verify>
  <done>
GUITestBase.cs exists with Display management, strict disposal checking, event queue verification, and diagnostic capture on failure.
  </done>
</task>

<task type="auto">
  <name>Task 3: Configure Coverlet coverage collection in CI</name>
  <files>.github/workflows/ci.yml, tests/SWTSharp.Tests/SWTSharp.Tests.csproj</files>
  <action>
Update CI workflow and test project for proper Coverlet coverage collection.

In .github/workflows/ci.yml, update the test command for each platform job:

```yaml
- name: Run tests with coverage
  run: |
    dotnet test tests/SWTSharp.Tests/SWTSharp.Tests.csproj \
      --configuration ${{ env.CONFIGURATION }} \
      --no-build \
      --logger "trx;LogFileName=test-results.trx" \
      --collect:"XPlat Code Coverage" \
      --results-directory ./TestResults
```

Add coverage merge step per RESEARCH.md Pattern 4:
- After all platform tests complete, download coverage artifacts
- Merge using Coverlet's MergeWith parameter
- Upload merged coverage to Codecov

In tests/SWTSharp.Tests/SWTSharp.Tests.csproj:
1. Ensure coverlet.collector is present (already is: Version 6.0.4)
2. Add runsettings file reference for coverage configuration:
   ```xml
   &lt;PropertyGroup&gt;
     &lt;CollectCoverage&gt;true&lt;/CollectCoverage&gt;
     &lt;CoverletOutputFormat&gt;cobertura&lt;/CoverletOutputFormat&gt;
     &lt;ExcludeByFile&gt;**/obj/**,**/bin/**&lt;/ExcludeByFile&gt;
   &lt;/PropertyGroup&gt;
   ```

Add a coverage merge job that runs after all platform tests:
```yaml
merge-coverage:
  needs: [test-windows, test-macos, test-linux]
  runs-on: ubuntu-latest
  steps:
    - uses: actions/download-artifact@v5
      with:
        pattern: test-results-*
        path: ./coverage
    - name: Upload to Codecov
      uses: codecov/codecov-action@v5
      with:
        directory: ./coverage
        fail_ci_if_error: false
```

Note: 80% coverage threshold is a goal for Phase 2 when more tests exist. Don't enforce threshold yet.
  </action>
  <verify>
Validate YAML syntax of ci.yml.
Build test project: `dotnet build tests/SWTSharp.Tests/SWTSharp.Tests.csproj`
  </verify>
  <done>
CI workflow collects coverage on all platforms with --collect:"XPlat Code Coverage", has merge-coverage job, and uploads to Codecov.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Build test infrastructure:
   ```bash
   dotnet build tests/SWTSharp.Tests/SWTSharp.Tests.csproj
   ```

2. Verify EventSyncHelpers contains RunContinuationsAsynchronously:
   ```bash
   grep -l "RunContinuationsAsynchronously" tests/SWTSharp.Tests/Infrastructure/EventSyncHelpers.cs
   ```

3. Verify GUITestBase contains disposal checking:
   ```bash
   grep -l "IsDisposed" tests/SWTSharp.Tests/Infrastructure/GUITestBase.cs
   ```

4. Verify CI has coverage collection:
   ```bash
   grep "XPlat Code Coverage" .github/workflows/ci.yml
   ```
</verification>

<success_criteria>
- EventSyncHelpers.cs exists with TaskCompletionSource and RunContinuationsAsynchronously
- GUITestBase.cs exists with Display management and strict disposal checking
- CI workflow has --collect:"XPlat Code Coverage" for all platforms
- CI workflow has merge-coverage job
- No polling loops (Thread.Sleep in while) in new synchronization helpers
- All files compile without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure-foundation/01-03-SUMMARY.md`
</output>
