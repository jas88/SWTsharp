---
phase: 01-test-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/SWTSharp.TestAdapter/SWTSharpTestExecutor.cs
  - tests/SWTSharp.TestHost/MainThreadDispatcher.cs
  - tests/SWTSharp.TestHost/Program.cs
autonomous: true

must_haves:
  truths:
    - "Tests run successfully on macOS via custom TestHost with Thread 1"
    - "Tests run successfully on Windows via xUnit execution engine"
    - "Tests run successfully on Linux via xUnit execution engine"
    - "Test results are correctly reported back to VSTest framework"
  artifacts:
    - path: "tests/SWTSharp.TestAdapter/SWTSharpTestExecutor.cs"
      provides: "Complete VSTest executor for all platforms"
      contains: "XunitFrontController"
    - path: "tests/SWTSharp.TestHost/MainThreadDispatcher.cs"
      provides: "Thread 1 dispatcher for macOS"
      contains: "dispatch_async_f"
  key_links:
    - from: "tests/SWTSharp.TestAdapter/SWTSharpTestExecutor.cs"
      to: "tests/SWTSharp.TestHost/Program.cs"
      via: "Process launch with stdout parsing"
      pattern: "ProcessStartInfo.*TestHost"
    - from: "tests/SWTSharp.TestAdapter/SWTSharpTestExecutor.cs"
      to: "xunit.runner.utility"
      via: "XunitFrontController for Windows/Linux"
      pattern: "XunitFrontController"
---

<objective>
Complete the custom VSTest adapter for running SWTSharp tests on all platforms.

Purpose: The existing VSTest adapter has a working macOS path but the Windows/Linux path returns "Skipped" with a TODO. This blocks test execution on 2 of 3 platforms.

Output: Fully functional SWTSharpTestExecutor that runs tests via xUnit on Windows/Linux in-process and via TestHost on macOS.
</objective>

<execution_context>
<!-- GSD workflow context - loaded from user's .claude directory -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-test-infrastructure-foundation/01-RESEARCH.md

Existing code to complete:
@tests/SWTSharp.TestAdapter/SWTSharpTestExecutor.cs
@tests/SWTSharp.TestHost/Program.cs
@tests/SWTSharp.TestHost/MainThreadDispatcher.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete Windows/Linux test executor</name>
  <files>tests/SWTSharp.TestAdapter/SWTSharpTestExecutor.cs</files>
  <action>
Replace the `RunTestsInDefaultHost` method stub with a working implementation that uses xUnit's `XunitFrontController` to discover and execute tests in-process.

Current code (lines 168-205) marks all tests as Skipped with "Default host implementation pending".

Implementation requirements:
1. Use XunitFrontController like the TestHost does (see tests/SWTSharp.TestHost/Program.cs lines 73-112)
2. Create TestExecutionVisitor to handle ITestPassed, ITestFailed, ITestSkipped messages
3. Map xUnit test results to VSTest TestResult objects via frameworkHandle.RecordResult()
4. Handle cancellation via the _cancelled flag
5. Set execution timeout to 30 seconds per test (use TestFrameworkOptions.ForExecution() with SetValue)

Do NOT use dispatch_sync_f or CFRunLoop on Windows/Linux - those are macOS-only patterns.

Reference the working pattern in TestHost/Program.cs but adapt it to report results through IFrameworkHandle instead of Console.WriteLine.
  </action>
  <verify>
Build the solution: `dotnet build tests/SWTSharp.TestAdapter/SWTSharp.TestAdapter.csproj`
Verify no compilation errors.
  </verify>
  <done>
RunTestsInDefaultHost method uses XunitFrontController to execute tests and reports results via frameworkHandle.RecordResult() with correct TestOutcome values.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add test timeout and deadlock detection to macOS host</name>
  <files>tests/SWTSharp.TestHost/Program.cs, tests/SWTSharp.TestHost/MainThreadDispatcher.cs</files>
  <action>
Add 30-second timeout per test with deadlock detection to the macOS test host.

In Program.cs TestExecutionVisitor:
1. Add a CancellationTokenSource with 30-second timeout per test
2. On ITestStarting, start the timeout timer
3. If timeout fires before ITestPassed/ITestFailed/ITestSkipped, log deadlock warning and fail the test
4. Use Thread.Interrupt() on the test thread if needed (last resort)

In MainThreadDispatcher.cs:
1. Ensure dispatch_async_f (not dispatch_sync_f) is used to avoid blocking caller
2. Add timeout parameter to Invoke method (default 30 seconds)
3. If timeout expires, log clear message: "[TIMEOUT] Test '{testName}' deadlocked after 30s - possible Thread 1 dispatch issue"

This follows the CONTEXT.md decision: "Timeout deadlocked tests at 30 seconds, kill and fail with clear message (no retry)"
  </action>
  <verify>
Build the solution: `dotnet build tests/SWTSharp.TestHost/SWTSharp.TestHost.csproj`
Verify no compilation errors.
  </verify>
  <done>
Tests that hang for more than 30 seconds are killed with a clear "[TIMEOUT]" message indicating deadlock, not left hanging indefinitely.
  </done>
</task>

</tasks>

<verification>
After completing both tasks:

1. Build entire test infrastructure:
   ```bash
   dotnet build tests/SWTSharp.TestAdapter/SWTSharp.TestAdapter.csproj
   dotnet build tests/SWTSharp.TestHost/SWTSharp.TestHost.csproj
   dotnet build tests/SWTSharp.Tests/SWTSharp.Tests.csproj
   ```

2. Verify Windows/Linux path compiles (no runtime test needed in this plan - that's for Plan 01-02 CI)

3. Check that executor no longer returns "Skipped" for RunTestsInDefaultHost
</verification>

<success_criteria>
- SWTSharpTestExecutor.RunTestsInDefaultHost uses XunitFrontController (not returning Skipped)
- TestHost has 30-second timeout per test with clear deadlock messages
- All test projects build without errors
- No new compiler warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure-foundation/01-01-SUMMARY.md`
</output>
