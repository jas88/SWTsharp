---
phase: 02-core-widget-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SWTSharp/Button.cs
  - src/SWTSharp/Label.cs
  - src/SWTSharp/Text.cs
  - src/SWTSharp/Shell.cs
  - src/SWTSharp/Composite.cs
  - src/SWTSharp/Platform/Win32/Win32Button.cs
  - src/SWTSharp/Platform/Linux/LinuxButton.cs
  - src/SWTSharp/Platform/MacOS/MacOSButton.cs
autonomous: true

must_haves:
  truths:
    - "Button.Selection property updates through platform widget on all platforms"
    - "Radio button groups correctly deselect siblings"
    - "Keyboard state mask reflects current modifier keys"
    - "All Button/Label/Text/Shell/Composite TODOs resolved"
  artifacts:
    - path: "src/SWTSharp/Button.cs"
      provides: "Complete Button implementation"
      contains: "IPlatformSelectionWidget"
    - path: "src/SWTSharp/Text.cs"
      provides: "Complete Text implementation"
      contains: "GetSelection()"
  key_links:
    - from: "Button.cs"
      to: "IPlatformSelectionWidget"
      via: "PlatformWidget cast"
      pattern: "IPlatformSelectionWidget"
---

<objective>
Complete all TODOs in core widget implementations (Button, Label, Text, Shell, Composite).

Purpose: These are the most fundamental widgets; their patterns establish the template for all other widget completions.
Output: Zero TODOs in Button.cs, Label.cs, Text.cs, Shell.cs, Composite.cs and their platform implementations.
</objective>

<execution_context>
<!-- GSD workflow context - loaded from user's .claude directory -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-widget-completion/02-RESEARCH.md
@.planning/phases/02-core-widget-completion/02-CONTEXT.md
@src/SWTSharp/Button.cs
@src/SWTSharp/Platform/IPlatformEventHandling.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete Button Selection and Radio Group Logic</name>
  <files>
    src/SWTSharp/Button.cs
    src/SWTSharp/Platform/IPlatformWidget.cs
  </files>
  <action>
    Resolve 4 TODOs in Button.cs:

    1. Line 59 - Selection property TODO:
       - Create IPlatformSelectionWidget interface with GetSelection()/SetSelection(bool) methods
       - Cast PlatformWidget and delegate to platform implementation
       - Fall back to internal _selection field if interface not implemented

    2. Line 142 - Radio button group deselection TODO:
       - Get parent Composite
       - Iterate Parent.Children to find sibling Radio buttons (same parent, SWT.RADIO style)
       - Set Selection = false on all siblings before setting self to true
       - Use Parent?.Children?.OfType<Button>().Where(b => b != this && (b.Style & SWT.RADIO) != 0)

    3. Line 228 - GetCurrentStateMask() TODO:
       - Add GetModifierKeyState() to IPlatformWidget interface
       - Implementation returns int with SWT.SHIFT, SWT.CTRL, SWT.ALT, SWT.COMMAND flags
       - Win32: Use GetKeyState() for VK_SHIFT, VK_CONTROL, VK_MENU
       - macOS: Use NSEvent.modifierFlags
       - Linux: Use gdk_keymap_get_modifier_state()

    4. Line 243 - Command key detection TODO:
       - In GetStateMaskFromPlatformArgs(), add Command key handling
       - Add Command property to PlatformKeyEventArgs
       - On macOS: if (e.Command) stateMask |= SWT.COMMAND
       - On other platforms: map Command to CTRL for compatibility
  </action>
  <verify>
    grep -c "TODO" src/SWTSharp/Button.cs returns 0
    dotnet build --nologo src/SWTSharp/SWTSharp.csproj
  </verify>
  <done>Button.cs has zero TODO comments and builds without errors</done>
</task>

<task type="auto">
  <name>Task 2: Complete Label and Text Widget TODOs</name>
  <files>
    src/SWTSharp/Label.cs
    src/SWTSharp/Text.cs
  </files>
  <action>
    Resolve TODOs in Label.cs (1 TODO) and Text.cs (12 TODOs):

    **Label.cs:**
    - Examine the TODO context and implement the missing functionality
    - Label is relatively simple; typically involves image handling or alignment

    **Text.cs (12 TODOs):**
    Common patterns to implement:

    1. Text selection handling:
       - Create IPlatformTextSelectionWidget with GetSelection(), SetSelection(Point), SelectAll()
       - Win32: EM_GETSEL, EM_SETSEL messages
       - macOS: NSTextField selectedRange/setSelectedRange
       - Linux: gtk_editable_get_selection_bounds/gtk_editable_select_region

    2. Clipboard operations:
       - Cut(), Copy(), Paste() methods through platform interface
       - Win32: WM_CUT, WM_COPY, WM_PASTE
       - macOS: NSResponder cut:, copy:, paste: actions
       - Linux: gtk_editable_cut_clipboard, etc.

    3. Undo/Redo if present:
       - Platform-dependent or custom implementation
       - Mark as SWT.ERROR_NOT_IMPLEMENTED if platform doesn't support

    4. Read-only and editable state synchronization:
       - Delegate to platform widget for actual enforcement
  </action>
  <verify>
    grep -c "TODO" src/SWTSharp/Label.cs returns 0
    grep -c "TODO" src/SWTSharp/Text.cs returns 0
    dotnet build --nologo src/SWTSharp/SWTSharp.csproj
  </verify>
  <done>Label.cs and Text.cs have zero TODO comments and build without errors</done>
</task>

<task type="auto">
  <name>Task 3: Complete Shell and Composite TODOs</name>
  <files>
    src/SWTSharp/Shell.cs
    src/SWTSharp/Composite.cs
  </files>
  <action>
    Resolve remaining TODOs in Shell.cs and Composite.cs:

    **Composite.cs (1 TODO):**
    - Examine the TODO context
    - Likely involves layout or child management
    - Implement using existing IPlatformWidget patterns

    **Shell.cs:**
    - Check for any TODOs (may be 0)
    - If present, typically involve:
      - Modal behavior implementation
      - Window state (minimize, maximize, fullscreen)
      - Window decoration/style application
      - Focus management

    For each TODO:
    1. Identify what platform interface method is needed
    2. Add to appropriate IPlatform* interface if not exists
    3. Implement in Win32, macOS, Linux platform classes
    4. Call from widget through PlatformWidget cast

    Follow the established pattern from Button.cs for platform delegation.
  </action>
  <verify>
    grep -c "TODO" src/SWTSharp/Composite.cs returns 0
    grep "TODO" src/SWTSharp/Shell.cs returns no matches (or 0 count)
    dotnet build --nologo src/SWTSharp/SWTSharp.csproj
  </verify>
  <done>Shell.cs and Composite.cs have zero TODO comments and build without errors</done>
</task>

</tasks>

<verification>
```bash
# Verify zero TODOs in core widgets
for f in Button.cs Label.cs Text.cs Shell.cs Composite.cs; do
  echo "$f: $(grep -c 'TODO' src/SWTSharp/$f 2>/dev/null || echo 0)"
done

# Verify build succeeds
dotnet build --nologo src/SWTSharp/SWTSharp.csproj

# Run any existing tests for these widgets
dotnet test tests/SWTSharp.Tests/SWTSharp.Tests.csproj --filter "FullyQualifiedName~Button|FullyQualifiedName~Label|FullyQualifiedName~Text|FullyQualifiedName~Shell|FullyQualifiedName~Composite" --no-build
```
</verification>

<success_criteria>
- Zero TODO comments in: Button.cs, Label.cs, Text.cs, Shell.cs, Composite.cs
- Zero TODO comments in platform implementations for these widgets
- All new interfaces have implementations for Win32, macOS, and Linux
- Build succeeds without errors
- Existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-widget-completion/02-01-SUMMARY.md`
</output>
