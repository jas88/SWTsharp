---
phase: 02-core-widget-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SWTSharp/TabFolder.cs
  - src/SWTSharp/TabItem.cs
  - src/SWTSharp/ToolBar.cs
  - src/SWTSharp/ToolItem.cs
  - src/SWTSharp/Menu.cs
  - src/SWTSharp/MenuItem.cs
  - src/SWTSharp/Group.cs
autonomous: true

must_haves:
  truths:
    - "TabFolder selection changes fire Selection events"
    - "ToolItem drop-down menus display correctly"
    - "MenuItem accelerators trigger keyboard shortcuts"
    - "Group text/border renders on all platforms"
  artifacts:
    - path: "src/SWTSharp/TabFolder.cs"
      provides: "Complete TabFolder implementation"
      contains: "SelectionChanged"
    - path: "src/SWTSharp/Menu.cs"
      provides: "Complete Menu implementation"
      contains: "SetVisible"
  key_links:
    - from: "ToolItem.cs"
      to: "Menu"
      via: "DropDown property"
      pattern: "DropDownMenu"
---

<objective>
Complete all TODOs in container widget implementations (TabFolder, ToolBar, Menu, Group).

Purpose: Container widgets organize child widgets; completing them enables proper UI composition.
Output: Zero TODOs in TabFolder, TabItem, ToolBar, ToolItem, Menu, MenuItem, and Group files.
</objective>

<execution_context>
<!-- GSD workflow context - loaded from user's .claude directory -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-widget-completion/02-RESEARCH.md
@src/SWTSharp/TabFolder.cs
@src/SWTSharp/ToolBar.cs
@src/SWTSharp/Menu.cs
@src/SWTSharp/Group.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete TabFolder and TabItem TODOs</name>
  <files>
    src/SWTSharp/TabFolder.cs
    src/SWTSharp/TabItem.cs
  </files>
  <action>
    Resolve TODOs in TabFolder.cs (2 TODOs) and TabItem.cs (2 TODOs):

    **TabFolder.cs common patterns:**
    1. Selection management:
       - Create IPlatformTabFolder with GetSelection(), SetSelection(int)
       - Fire SWT.Selection event when tab changes
       - Win32: TCM_GETCURSEL, TCM_SETCURSEL
       - macOS: NSTabView selectedTabViewItem
       - Linux: gtk_notebook_get_current_page, gtk_notebook_set_current_page

    2. Tab ordering/removal:
       - GetItems() returns TabItem array in order
       - RemoveItem(TabItem) cleans up platform resources

    **TabItem.cs common patterns:**
    1. Control association:
       - SetControl(Control) associates content with tab
       - Platform shows/hides controls when tab selected

    2. Image/text updates:
       - SetImage(), SetText() update tab appearance
       - Delegate to platform widget

    For each TODO, follow the Button.cs pattern:
    - Identify platform interface needed
    - Add to/create IPlatformTabFolder or IPlatformTabItem
    - Implement in platform classes
    - Delegate from widget through PlatformWidget cast
  </action>
  <verify>
    grep -c "TODO" src/SWTSharp/TabFolder.cs returns 0
    grep -c "TODO" src/SWTSharp/TabItem.cs returns 0
    dotnet build --nologo src/SWTSharp/SWTSharp.csproj
  </verify>
  <done>TabFolder.cs and TabItem.cs have zero TODO comments</done>
</task>

<task type="auto">
  <name>Task 2: Complete ToolBar and ToolItem TODOs</name>
  <files>
    src/SWTSharp/ToolBar.cs
    src/SWTSharp/ToolItem.cs
  </files>
  <action>
    Resolve TODOs in ToolBar.cs (9 TODOs) and ToolItem.cs (4 TODOs):

    **ToolBar.cs patterns:**
    1. Item layout:
       - GetItems() returns ToolItem array
       - ComputeSize() calculates toolbar dimensions
       - Horizontal vs vertical orientation (SWT.HORIZONTAL, SWT.VERTICAL)

    2. Wrap behavior:
       - SWT.WRAP style causes multi-row layout
       - Track row breaks in item positions

    **ToolItem.cs patterns:**
    1. Item types (SWT.PUSH, SWT.CHECK, SWT.RADIO, SWT.SEPARATOR, SWT.DROP_DOWN):
       - PUSH: Simple click button
       - CHECK/RADIO: Toggle state with Selection property
       - SEPARATOR: Spacing element
       - DROP_DOWN: Button with attached Menu

    2. Drop-down menu:
       - DropDownMenu property holds Menu
       - Arrow click shows menu at item location
       - Create IPlatformToolItem with ShowDropDown() method

    3. Hot image vs normal image:
       - HotImage shown on hover
       - DisabledImage when not enabled

    4. Width for SEPARATOR:
       - SEPARATOR items can have custom width
       - Platform must respect SetWidth()

    Create IPlatformToolBar and IPlatformToolItem interfaces as needed.
  </action>
  <verify>
    grep -c "TODO" src/SWTSharp/ToolBar.cs returns 0
    grep -c "TODO" src/SWTSharp/ToolItem.cs returns 0
    dotnet build --nologo src/SWTSharp/SWTSharp.csproj
  </verify>
  <done>ToolBar.cs and ToolItem.cs have zero TODO comments</done>
</task>

<task type="auto">
  <name>Task 3: Complete Menu, MenuItem, and Group TODOs</name>
  <files>
    src/SWTSharp/Menu.cs
    src/SWTSharp/MenuItem.cs
    src/SWTSharp/Group.cs
  </files>
  <action>
    Resolve TODOs in Menu.cs (5 TODOs), MenuItem.cs (7 TODOs), and Group.cs (13 TODOs):

    **Menu.cs patterns:**
    1. Menu types:
       - SWT.BAR: Menu bar attached to Shell
       - SWT.DROP_DOWN: Submenu or context menu
       - SWT.POP_UP: Context menu shown at location

    2. Visibility:
       - SetVisible(bool) shows/hides popup menus
       - SetLocation(x, y) positions popup
       - Win32: TrackPopupMenu
       - macOS: popUpMenuPositioningItem
       - Linux: gtk_menu_popup_at_pointer

    3. Default item:
       - DefaultItem property for Enter key activation

    **MenuItem.cs patterns:**
    1. Item types (SWT.PUSH, SWT.CHECK, SWT.RADIO, SWT.CASCADE, SWT.SEPARATOR):
       - CASCADE: Has submenu (Menu property)
       - CHECK/RADIO: Toggle with Selection property

    2. Accelerator:
       - Accelerator property is key combination (SWT.CTRL | 'S')
       - Platform must register keyboard shortcut
       - Format display text with accelerator hint

    3. Image support:
       - SetImage() for menu item icons

    **Group.cs patterns (13 TODOs):**
    1. Border and title:
       - Group is a labeled frame container
       - Text property sets title
       - Platform renders native group box

    2. Client area:
       - GetClientArea() returns inner rectangle
       - Must account for title height and border
       - Win32: WS_EX_CONTROLPARENT style
       - macOS: NSBox
       - Linux: GtkFrame

    3. Layout management:
       - Group inherits from Composite
       - Layout applies to client area, not full bounds

    Create IPlatformMenu, IPlatformMenuItem, IPlatformGroup as needed.
  </action>
  <verify>
    grep -c "TODO" src/SWTSharp/Menu.cs returns 0
    grep -c "TODO" src/SWTSharp/MenuItem.cs returns 0
    grep -c "TODO" src/SWTSharp/Group.cs returns 0
    dotnet build --nologo src/SWTSharp/SWTSharp.csproj
  </verify>
  <done>Menu.cs, MenuItem.cs, and Group.cs have zero TODO comments</done>
</task>

</tasks>

<verification>
```bash
# Verify zero TODOs in container widgets
for f in TabFolder.cs TabItem.cs ToolBar.cs ToolItem.cs Menu.cs MenuItem.cs Group.cs; do
  echo "$f: $(grep -c 'TODO' src/SWTSharp/$f 2>/dev/null || echo 0)"
done

# Verify build succeeds
dotnet build --nologo src/SWTSharp/SWTSharp.csproj

# Run existing tests
dotnet test tests/SWTSharp.Tests/SWTSharp.Tests.csproj --filter "FullyQualifiedName~TabFolder|FullyQualifiedName~ToolBar|FullyQualifiedName~Menu|FullyQualifiedName~Group" --no-build
```
</verification>

<success_criteria>
- Zero TODO comments in: TabFolder.cs, TabItem.cs, ToolBar.cs, ToolItem.cs, Menu.cs, MenuItem.cs, Group.cs
- Platform implementations complete for Win32, macOS, Linux
- Build succeeds without errors
- Existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-widget-completion/02-02-SUMMARY.md`
</output>
