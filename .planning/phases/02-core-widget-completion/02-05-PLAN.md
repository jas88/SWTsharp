---
phase: 02-core-widget-completion
plan: 05
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/SWTSharp/Table.cs
  - src/SWTSharp/TableColumn.cs
  - src/SWTSharp/TableItem.cs
  - src/SWTSharp/Tree.cs
  - src/SWTSharp/TreeItem.cs
  - src/SWTSharp/Combo.cs
  - src/SWTSharp/List.cs
autonomous: true

must_haves:
  truths:
    - "Table supports column headers and sorting"
    - "Tree supports hierarchical items with expand/collapse"
    - "Combo supports both dropdown and editable modes"
    - "List supports single and multi-selection"
  artifacts:
    - path: "src/SWTSharp/Table.cs"
      provides: "Complete Table implementation"
      contains: "GetColumn|SetSelection"
    - path: "src/SWTSharp/Tree.cs"
      provides: "Complete Tree implementation"
      contains: "GetItems|SetExpanded"
  key_links:
    - from: "Table.cs"
      to: "TableColumn.cs"
      via: "column management"
      pattern: "GetColumns"
---

<objective>
Complete all TODOs in complex widget implementations (Table, Tree, Combo, List).

Purpose: These widgets display and manage collections; they're essential for data-driven UIs.
Output: Zero TODOs in Table, TableColumn, TableItem, Tree, TreeItem, Combo, and List files.
</objective>

<execution_context>
<!-- GSD workflow context - loaded from user's .claude directory -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-widget-completion/02-RESEARCH.md
@.planning/phases/02-core-widget-completion/02-01-SUMMARY.md
@src/SWTSharp/Table.cs
@src/SWTSharp/Tree.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete Table, TableColumn, and TableItem</name>
  <files>
    src/SWTSharp/Table.cs
    src/SWTSharp/TableColumn.cs
    src/SWTSharp/TableItem.cs
  </files>
  <action>
    Resolve TODOs in Table.cs (1), TableColumn.cs (12), and TableItem.cs (2):

    **Table.cs patterns:**
    1. Column management:
       - GetColumns() returns TableColumn array
       - GetColumn(index) returns specific column
       - SetColumnOrder(int[]) reorders columns

    2. Item management:
       - GetItems() returns TableItem array
       - GetItem(index) returns specific item
       - GetSelectionIndices() for multi-select
       - SetSelection(int[]) for multi-select

    3. Virtual table (SWT.VIRTUAL):
       - SetItemCount(n) preallocates
       - SWT.SetData event for lazy loading

    **TableColumn.cs patterns (12 TODOs):**
    1. Column properties:
       - Width get/set through platform
       - Alignment (SWT.LEFT, SWT.CENTER, SWT.RIGHT)
       - Moveable, Resizable flags

    2. Column events:
       - SWT.Selection for header click (sorting)
       - SWT.Move for reorder
       - SWT.Resize for width change

    3. Pack (auto-size):
       - Pack() sizes to content
       - Pack(bool) includes header

    **TableItem.cs patterns (2 TODOs):**
    1. Multi-column text:
       - GetText(column) / SetText(column, text)
       - GetImage(column) / SetImage(column, image)

    2. Styling:
       - SetBackground(column, color)
       - SetForeground(column, color)
       - SetFont(column, font)

    Create IPlatformTable, IPlatformTableColumn, IPlatformTableItem as needed.
    Pattern from Plan 01 (Button.cs) for platform delegation.
  </action>
  <verify>
    grep -c "TODO" src/SWTSharp/Table.cs returns 0
    grep -c "TODO" src/SWTSharp/TableColumn.cs returns 0
    grep -c "TODO" src/SWTSharp/TableItem.cs returns 0
    dotnet build --nologo src/SWTSharp/SWTSharp.csproj
  </verify>
  <done>Table, TableColumn, and TableItem have zero TODO comments</done>
</task>

<task type="auto">
  <name>Task 2: Complete Tree and TreeItem</name>
  <files>
    src/SWTSharp/Tree.cs
    src/SWTSharp/TreeItem.cs
  </files>
  <action>
    Resolve TODOs in Tree.cs (4) and TreeItem.cs (5):

    **Tree.cs patterns:**
    1. Item management:
       - GetItems() returns root TreeItem array
       - GetSelection() returns selected items
       - SetSelection(TreeItem[]) sets selection

    2. Columns (Tree can have columns like Table):
       - GetColumns(), GetColumn(index)
       - SetHeaderVisible(bool)

    3. Expand/Collapse:
       - ExpandAll(), CollapseAll() (recursive)
       - SWT.Expand, SWT.Collapse events
       - SWT.VIRTUAL for lazy loading

    **TreeItem.cs patterns:**
    1. Hierarchy:
       - GetParentItem() returns parent (null for root)
       - GetItems() returns children
       - GetItemCount() returns child count

    2. Expand state:
       - Expanded property (get/set)
       - SetExpanded(bool) triggers platform

    3. Multi-column (when Tree has columns):
       - GetText(column) / SetText(column, text)
       - GetImage(column) / SetImage(column, image)

    4. Styling (per cell):
       - SetBackground(column, color)
       - SetForeground(column, color)

    Platform implementations:
    - Win32: TreeView control with column extension
    - macOS: NSOutlineView
    - Linux: GtkTreeView with GtkTreeStore

    Create IPlatformTree, IPlatformTreeItem as needed.
  </action>
  <verify>
    grep -c "TODO" src/SWTSharp/Tree.cs returns 0
    grep -c "TODO" src/SWTSharp/TreeItem.cs returns 0
    dotnet build --nologo src/SWTSharp/SWTSharp.csproj
  </verify>
  <done>Tree and TreeItem have zero TODO comments</done>
</task>

<task type="auto">
  <name>Task 3: Complete Combo and List</name>
  <files>
    src/SWTSharp/Combo.cs
    src/SWTSharp/List.cs
  </files>
  <action>
    Resolve TODOs in Combo.cs (10) and List.cs (4):

    **Combo.cs patterns (10 TODOs):**
    1. Combo types:
       - SWT.DROP_DOWN: Editable with dropdown
       - SWT.READ_ONLY: Selection only, no typing
       - SWT.SIMPLE: Always shows list (like List)

    2. Item management:
       - Add(string), Add(string, index)
       - Remove(index), RemoveAll()
       - GetItems() returns string array
       - SetItems(string[]) replaces all

    3. Selection:
       - GetSelectionIndex() / SetSelectionIndex()
       - GetText() / SetText() for editable text
       - Select(index), Deselect(index)

    4. Events:
       - SWT.Selection when item selected
       - SWT.Modify when text edited
       - SWT.DefaultSelection on Enter

    5. Dropdown:
       - SetVisibleItemCount(n) sets dropdown height
       - GetVisibleItemCount()

    **List.cs patterns (4 TODOs):**
    1. Selection modes:
       - SWT.SINGLE: One selection
       - SWT.MULTI: Multiple selection

    2. Item management:
       - Add(string), Add(string, index)
       - Remove(index), RemoveAll()
       - GetItems() returns string array
       - SetItems(string[]) replaces all

    3. Selection:
       - GetSelectionIndex() for single
       - GetSelectionIndices() for multi
       - SetSelection(int[]) for multi
       - SelectAll(), DeselectAll()

    4. Visibility:
       - ShowSelection() scrolls to selected
       - GetTopIndex() / SetTopIndex()

    Platform implementations:
    - Win32: ComboBox / ListBox controls
    - macOS: NSComboBox / NSTableView
    - Linux: GtkComboBox / GtkListBox or GtkTreeView

    Create IPlatformCombo, IPlatformList as needed.
  </action>
  <verify>
    grep -c "TODO" src/SWTSharp/Combo.cs returns 0
    grep -c "TODO" src/SWTSharp/List.cs returns 0
    dotnet build --nologo src/SWTSharp/SWTSharp.csproj
  </verify>
  <done>Combo and List have zero TODO comments</done>
</task>

</tasks>

<verification>
```bash
# Verify zero TODOs in complex widgets
for f in Table.cs TableColumn.cs TableItem.cs Tree.cs TreeItem.cs Combo.cs List.cs; do
  echo "$f: $(grep -c 'TODO' src/SWTSharp/$f 2>/dev/null || echo 0)"
done

# Verify build succeeds
dotnet build --nologo src/SWTSharp/SWTSharp.csproj

# Run tests
dotnet test tests/SWTSharp.Tests/SWTSharp.Tests.csproj --filter "FullyQualifiedName~Table|FullyQualifiedName~Tree|FullyQualifiedName~Combo|FullyQualifiedName~List" --no-build
```
</verification>

<success_criteria>
- Zero TODO comments in: Table.cs, TableColumn.cs, TableItem.cs, Tree.cs, TreeItem.cs, Combo.cs, List.cs
- Platform implementations complete for all three platforms
- Column/item management works (add, remove, reorder)
- Selection works (single and multi where applicable)
- Events fire correctly (Selection, Modify, Expand, Collapse)
- Build succeeds without errors
- Existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-widget-completion/02-05-SUMMARY.md`
</output>
